# 云服务系统数据库设计

## 系统概述

本系统为用户提供云服务器、容器、存储、网络等基础设施服务，支持资源管理、订单计费、监控告警等功能。

## 核心表结构设计

### 用户与账户模块

```sql
-- 用户基础信息表
CREATE TABLE t_cloud_user (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '用户ID',
    username VARCHAR(50) NOT NULL COMMENT '用户名',
    password VARCHAR(255) NOT NULL COMMENT '密码',
    email VARCHAR(100) NOT NULL COMMENT '邮箱',
    phone VARCHAR(20) DEFAULT NULL COMMENT '手机号',
    real_name VARCHAR(100) DEFAULT NULL COMMENT '真实姓名',
    id_card VARCHAR(20) DEFAULT NULL COMMENT '身份证号',
    company VARCHAR(200) DEFAULT NULL COMMENT '公司名称',
    status TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-禁用，1-正常，2-待验证',
    user_type TINYINT NOT NULL DEFAULT 1 COMMENT '类型：1-个人，2-企业',
    last_login_at DATETIME DEFAULT NULL COMMENT '最后登录时间',
    last_login_ip VARCHAR(50) DEFAULT NULL COMMENT '最后登录IP',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '注册时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted_at DATETIME DEFAULT NULL COMMENT '删除时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_username (username),
    UNIQUE KEY uk_email (email),
    UNIQUE KEY uk_phone (phone)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 租户表（用于多租户隔离）
CREATE TABLE t_cloud_tenant (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '租户ID',
    tenant_name VARCHAR(100) NOT NULL COMMENT '租户名称',
    tenant_code VARCHAR(50) NOT NULL COMMENT '租户代码',
    tenant_type TINYINT NOT NULL DEFAULT 1 COMMENT '类型：1-免费，2-付费，3-VIP',
    quota JSON DEFAULT NULL COMMENT '资源配额JSON',
    balance DECIMAL(12,2) NOT NULL DEFAULT 0.00 COMMENT '账户余额',
    credit_limit DECIMAL(12,2) NOT NULL DEFAULT 0.00 COMMENT '信用额度',
    status TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-停用，1-正常',
    expire_time DATETIME DEFAULT NULL COMMENT '到期时间',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_tenant_code (tenant_code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='租户表';

-- 用户租户关联表
CREATE TABLE t_cloud_user_tenant (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT 'ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    tenant_id BIGINT NOT NULL COMMENT '租户ID',
    is_owner TINYINT NOT NULL DEFAULT 0 COMMENT '是否所有者：0-否，1-是',
    role VARCHAR(50) NOT NULL DEFAULT 'member' COMMENT '角色：owner/admin/member',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_user_tenant (user_id, tenant_id),
    KEY idx_tenant_id (tenant_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户租户关联表';

-- 资源配额表
CREATE TABLE t_cloud_quota (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '配额ID',
    tenant_id BIGINT NOT NULL COMMENT '租户ID',
    resource_type VARCHAR(50) NOT NULL COMMENT '资源类型：vm/container/disk/bandwidth/ip',
    quota_limit INT NOT NULL COMMENT '配额上限',
    quota_used INT NOT NULL DEFAULT 0 COMMENT '已使用量',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_tenant_resource (tenant_id, resource_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='资源配额表';
```

### 地域与可用区模块

```sql
-- 地域表
CREATE TABLE t_cloud_region (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '地域ID',
    region_code VARCHAR(50) NOT NULL COMMENT '地域代码',
    region_name VARCHAR(100) NOT NULL COMMENT '地域名称',
    region_name_en VARCHAR(100) DEFAULT NULL COMMENT '英文名称',
    continent VARCHAR(50) NOT NULL COMMENT '所属大洲',
    country VARCHAR(50) NOT NULL COMMENT '国家',
    city VARCHAR(50) DEFAULT NULL COMMENT '城市',
    location VARCHAR(200) DEFAULT NULL COMMENT '具体位置',
    status TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-不可用，1-可用',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_region_code (region_code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='地域表';

-- 可用区表
CREATE TABLE t_cloud_zone (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '可用区ID',
    zone_code VARCHAR(50) NOT NULL COMMENT '可用区代码',
    zone_name VARCHAR(100) NOT NULL COMMENT '可用区名称',
    region_id BIGINT NOT NULL COMMENT '所属地域ID',
    zone_type TINYINT NOT NULL DEFAULT 1 COMMENT '类型：1-标准区，2-专属区',
    status TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-不可用，1-可用',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_zone_code (zone_code),
    KEY idx_region_id (region_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='可用区表';
```

### 镜像模块

```sql
-- 操作系统镜像表
CREATE TABLE t_cloud_image (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '镜像ID',
    image_code VARCHAR(100) NOT NULL COMMENT '镜像代码',
    image_name VARCHAR(200) NOT NULL COMMENT '镜像名称',
    image_type TINYINT NOT NULL COMMENT '类型：1-系统镜像，2-自定义镜像，3-共享镜像',
    os_type VARCHAR(50) NOT NULL COMMENT '操作系统类型：centos/ubuntu/windows',
    os_version VARCHAR(50) NOT NULL COMMENT '系统版本',
    os_arch VARCHAR(20) NOT NULL DEFAULT 'x86_64' COMMENT '系统架构',
    disk_type VARCHAR(50) NOT NULL COMMENT '磁盘类型',
    disk_size INT NOT NULL COMMENT '系统盘大小（GB）',
    image_url VARCHAR(500) NOT NULL COMMENT '镜像存储地址',
    image_size BIGINT NOT NULL COMMENT '镜像大小（字节）',
    snapshot_id BIGINT DEFAULT NULL COMMENT '关联快照ID（自定义镜像）',
    tenant_id BIGINT DEFAULT NULL COMMENT '所有者租户ID（NULL表示公共镜像）',
    is_available TINYINT NOT NULL DEFAULT 1 COMMENT '是否可用：0-不可用，1-可用',
    is_deleted TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted_at DATETIME DEFAULT NULL COMMENT '删除时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_image_code (image_code),
    KEY idx_os_type (os_type),
    KEY idx_tenant_id (tenant_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='操作系统镜像表';
```

### 规格与定价模块

```sql
-- 云服务器规格表
CREATE TABLE t_cloud_instance_type (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '规格ID',
    type_code VARCHAR(50) NOT NULL COMMENT '规格代码',
    type_name VARCHAR(100) NOT NULL COMMENT '规格名称',
    cpu_cores INT NOT NULL COMMENT 'CPU核心数',
    memory_size INT NOT NULL COMMENT '内存大小（MB）',
    disk_size INT DEFAULT 0 COMMENT '系统盘大小（GB）',
    disk_type VARCHAR(50) DEFAULT NULL COMMENT '系统盘类型',
    max_data_disks INT NOT NULL DEFAULT 0 COMMENT '最大数据盘数量',
    max_bandwidth INT DEFAULT NULL COMMENT '最大带宽（Mbps）',
    max_pps INT DEFAULT NULL COMMENT '最大每秒包数',
    network_capacity INT DEFAULT NULL COMMENT '网络收发包能力（万PPS）',
    type_family VARCHAR(50) DEFAULT NULL COMMENT '规格族',
    type_category TINYINT NOT NULL DEFAULT 1 COMMENT '类别：1-通用型，2-计算型，3-内存型，4-存储型，5-GPU型',
    status TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-停售，1-在售',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_type_code (type_code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='云服务器规格表';

-- 容器集群规格表
CREATE TABLE t_cloud_container_spec (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '规格ID',
    spec_code VARCHAR(50) NOT NULL COMMENT '规格代码',
    spec_name VARCHAR(100) NOT NULL COMMENT '规格名称',
    cpu_cores DECIMAL(5,2) NOT NULL COMMENT 'CPU核心数',
    memory_size INT NOT NULL COMMENT '内存大小（MB）',
    pod_quantity INT NOT NULL DEFAULT 1 COMMENT 'Pod数量',
    ephemeral_storage INT DEFAULT NULL COMMENT '临时存储（MB）',
    gpu_type VARCHAR(50) DEFAULT NULL COMMENT 'GPU类型',
    gpu_count INT DEFAULT 0 COMMENT 'GPU数量',
    status TINYINT NOT NULL DEFAULT 1 COMMENT '状态',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_spec_code (spec_code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='容器集群规格表';

-- 资源定价表
CREATE TABLE t_cloud_pricing (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '定价ID',
    pricing_type VARCHAR(50) NOT NULL COMMENT '计费类型：vm/container/disk/bandwidth/snapshot',
    region_id BIGINT NOT NULL COMMENT '地域ID',
    zone_id BIGINT DEFAULT NULL COMMENT '可用区ID（NULL表示地域级别）',
    spec_id BIGINT DEFAULT NULL COMMENT '规格ID（NULL表示按量计费）',
    price_type TINYINT NOT NULL COMMENT '计费方式：1-按量付费，2-包年包月',
    price_unit VARCHAR(50) NOT NULL COMMENT '价格单位：hour/month/year',
    unit_price DECIMAL(10,4) NOT NULL COMMENT '单价',
    currency VARCHAR(10) NOT NULL DEFAULT 'CNY' COMMENT '货币',
    status TINYINT NOT NULL DEFAULT 1 COMMENT '状态',
    effective_from DATETIME NOT NULL COMMENT '生效时间',
    effective_to DATETIME DEFAULT NULL COMMENT '失效时间',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    KEY idx_pricing_type_region (pricing_type, region_id),
    KEY idx_effective_date (effective_from, effective_to)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='资源定价表';

-- 优惠券表
CREATE TABLE t_cloud_coupon (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '优惠券ID',
    coupon_code VARCHAR(50) NOT NULL COMMENT '优惠券码',
    coupon_name VARCHAR(200) NOT NULL COMMENT '优惠券名称',
    coupon_type TINYINT NOT NULL COMMENT '类型：1-满减，2-折扣，3-兑换',
    discount_value DECIMAL(10,2) NOT NULL COMMENT '优惠值',
    min_order_amount DECIMAL(10,2) DEFAULT 0.00 COMMENT '最低订单金额',
    max_discount_amount DECIMAL(10,2) DEFAULT NULL COMMENT '最高优惠金额',
    valid_from DATETIME NOT NULL COMMENT '有效期开始',
    valid_until DATETIME NOT NULL COMMENT '有效期结束',
    status TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-未激活，1-待使用，2-已使用，3-已过期',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_coupon_code (coupon_code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='优惠券表';

-- 用户优惠券表
CREATE TABLE t_cloud_user_coupon (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT 'ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    coupon_id BIGINT NOT NULL COMMENT '优惠券ID',
    order_id BIGINT DEFAULT NULL COMMENT '关联订单ID',
    used_at DATETIME DEFAULT NULL COMMENT '使用时间',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '领取时间',
    PRIMARY KEY (id),
    KEY idx_user_id (user_id),
    KEY idx_order_id (order_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户优惠券表';
```

### 云服务器模块

```sql
-- 云服务器表
CREATE TABLE t_cloud_instance (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '实例ID',
    instance_id VARCHAR(100) NOT NULL COMMENT '实例标识（i-xxxxx）',
    instance_name VARCHAR(200) NOT NULL COMMENT '实例名称',
    tenant_id BIGINT NOT NULL COMMENT '所属租户ID',
    region_id BIGINT NOT NULL COMMENT '地域ID',
    zone_id BIGINT NOT NULL COMMENT '可用区ID',
    instance_type_id BIGINT NOT NULL COMMENT '规格ID',
    image_id BIGINT NOT NULL COMMENT '镜像ID',
    vpc_id BIGINT NOT NULL COMMENT '所属VPC ID',
    subnet_id BIGINT DEFAULT NULL COMMENT '所属子网ID',
    private_ip VARCHAR(50) DEFAULT NULL COMMENT '内网IP',
    public_ip VARCHAR(50) DEFAULT NULL COMMENT '公网IP',
    bandwidth INT DEFAULT NULL COMMENT '公网带宽（Mbps）',
    security_group_ids VARCHAR(500) DEFAULT NULL COMMENT '安全组ID列表（JSON）',
    instance_status VARCHAR(50) NOT NULL COMMENT '状态：pending/running/stopping/stopped/resetting/rebooting',
    instance_charge_type TINYINT NOT NULL COMMENT '计费方式：1-按量付费，2-包年包月',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    expired_at DATETIME DEFAULT NULL COMMENT '到期时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted_at DATETIME DEFAULT NULL COMMENT '删除时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_instance_id (instance_id),
    KEY idx_tenant_id (tenant_id),
    KEY idx_region_zone (region_id, zone_id),
    KEY idx_status (instance_status),
    KEY idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='云服务器表';

-- 云服务器操作记录表
CREATE TABLE t_cloud_instance_operation (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT 'ID',
    instance_id VARCHAR(100) NOT NULL COMMENT '实例标识',
    operation_type VARCHAR(50) NOT NULL COMMENT '操作类型：start/stop/reboot/reset/renew/resize',
    operation_status VARCHAR(50) NOT NULL COMMENT '状态：pending/success/failed',
    request_params JSON DEFAULT NULL COMMENT '请求参数',
    error_message TEXT DEFAULT NULL COMMENT '错误信息',
    operator_id BIGINT DEFAULT NULL COMMENT '操作人ID',
    operator_name VARCHAR(100) DEFAULT NULL COMMENT '操作人名称',
    started_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '开始时间',
    finished_at DATETIME DEFAULT NULL COMMENT '完成时间',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (id),
    KEY idx_instance_id (instance_id),
    KEY idx_operation_type (operation_type)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='云服务器操作记录表';

-- 云服务器监控数据表
CREATE TABLE t_cloud_instance_metric (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT 'ID',
    instance_id VARCHAR(100) NOT NULL COMMENT '实例标识',
    metric_time DATETIME NOT NULL COMMENT '采集时间',
    cpu_usage DECIMAL(5,2) DEFAULT 0.00 COMMENT 'CPU使用率%',
    memory_usage DECIMAL(5,2) DEFAULT 0.00 COMMENT '内存使用率%',
    disk_read_iops INT DEFAULT 0 COMMENT '磁盘读IOPS',
    disk_write_iops INT DEFAULT 0 COMMENT '磁盘写IOPS',
    disk_read_throughput BIGINT DEFAULT 0 COMMENT '磁盘读吞吐量（KB/s）',
    disk_write_throughput BIGINT DEFAULT 0 COMMENT '磁盘写吞吐量（KB/s）',
    network_in_rate BIGINT DEFAULT 0 COMMENT '网络入速率（Kbps）',
    network_out_rate BIGINT DEFAULT 0 COMMENT '网络出速率（Kbps）',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_instance_time (instance_id, metric_time),
    KEY idx_metric_time (metric_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='云服务器监控数据表';
```

### 容器与集群模块

```sql
-- 容器集群表
CREATE TABLE t_cloud_container_cluster (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '集群ID',
    cluster_id VARCHAR(100) NOT NULL COMMENT '集群标识（cc-xxxxx）',
    cluster_name VARCHAR(200) NOT NULL COMMENT '集群名称',
    cluster_type TINYINT NOT NULL COMMENT '类型：1-标准集群，2-专属集群',
    tenant_id BIGINT NOT NULL COMMENT '所属租户ID',
    region_id BIGINT NOT NULL COMMENT '地域ID',
    zone_ids VARCHAR(200) NOT NULL COMMENT '可用区列表（JSON）',
    vpc_id BIGINT NOT NULL COMMENT '所属VPC ID',
    pod_cidr VARCHAR(50) NOT NULL COMMENT 'Pod CIDR',
    service_cidr VARCHAR(50) NOT NULL COMMENT 'Service CIDR',
    master_count INT NOT NULL COMMENT 'Master节点数',
    master_spec_id BIGINT NOT NULL COMMENT 'Master规格ID',
    worker_count INT NOT NULL DEFAULT 0 COMMENT 'Worker节点数',
    runtime_version VARCHAR(50) DEFAULT NULL COMMENT '容器运行时版本',
    network_plugin VARCHAR(50) DEFAULT NULL COMMENT '网络插件',
    cluster_status VARCHAR(50) NOT NULL COMMENT '状态：creating/running/scaling/updating/deleting/failed',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted_at DATETIME DEFAULT NULL COMMENT '删除时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_cluster_id (cluster_id),
    KEY idx_tenant_id (tenant_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='容器集群表';

-- 集群节点表
CREATE TABLE t_cloud_container_node (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '节点ID',
    node_id VARCHAR(100) NOT NULL COMMENT '节点标识',
    cluster_id BIGINT NOT NULL COMMENT '所属集群ID',
    node_name VARCHAR(200) NOT NULL COMMENT '节点名称',
    tenant_id BIGINT NOT NULL COMMENT '所属租户ID',
    region_id BIGINT NOT NULL COMMENT '地域ID',
    zone_id BIGINT NOT NULL COMMENT '可用区ID',
    spec_id BIGINT NOT NULL COMMENT '规格ID',
    private_ip VARCHAR(50) NOT NULL COMMENT '内网IP',
    instance_id VARCHAR(100) DEFAULT NULL COMMENT '关联云服务器ID',
    node_status VARCHAR(50) NOT NULL COMMENT '状态：creating/pending/running/scheduling-disabled/paused/deleting',
    node_role TINYINT NOT NULL COMMENT '角色：1-Master，2-Worker',
    pod_count INT NOT NULL DEFAULT 0 COMMENT '当前Pod数量',
    max_pod_count INT NOT NULL DEFAULT 110 COMMENT '最大Pod数量',
    cpu_allocatable DECIMAL(5,2) DEFAULT 0.00 COMMENT '可分配CPU',
    memory_allocatable INT DEFAULT 0 COMMENT '可分配内存（MB）',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted_at DATETIME DEFAULT NULL COMMENT '删除时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_node_id (node_id),
    KEY idx_cluster_id (cluster_id),
    KEY idx_instance_id (instance_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='集群节点表';

-- Kubernetes命名空间表
CREATE TABLE t_cloud_k8s_namespace (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '命名空间ID',
    cluster_id BIGINT NOT NULL COMMENT '集群ID',
    namespace VARCHAR(100) NOT NULL COMMENT '命名空间名称',
    display_name VARCHAR(200) DEFAULT NULL COMMENT '显示名称',
    description VARCHAR(500) DEFAULT NULL COMMENT '描述',
    quota JSON DEFAULT NULL COMMENT '资源配额',
    tenant_id BIGINT NOT NULL COMMENT '所属租户ID',
    status TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-终止中，1-活跃',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_cluster_namespace (cluster_id, namespace),
    KEY idx_tenant_id (tenant_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='K8s命名空间表';

-- 容器Pod表
CREATE TABLE t_cloud_container_pod (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT 'Pod ID',
    pod_id VARCHAR(100) NOT NULL COMMENT 'Pod标识',
    cluster_id BIGINT NOT NULL COMMENT '集群ID',
    namespace_id BIGINT NOT NULL COMMENT '命名空间ID',
    tenant_id BIGINT NOT NULL COMMENT '所属租户ID',
    node_id BIGINT DEFAULT NULL COMMENT '调度节点ID',
    pod_name VARCHAR(200) NOT NULL COMMENT 'Pod名称',
    pod_ip VARCHAR(50) DEFAULT NULL COMMENT 'Pod IP',
    spec_id BIGINT NOT NULL COMMENT '规格ID',
    image_urls TEXT NOT NULL COMMENT '镜像URL列表',
    replicas INT NOT NULL DEFAULT 1 COMMENT '副本数',
    current_replicas INT NOT NULL DEFAULT 0 COMMENT '当前副本数',
    status VARCHAR(50) NOT NULL COMMENT '状态：Pending/Running/Succeeded/Failed/Unknown',
    restart_count INT NOT NULL DEFAULT 0 COMMENT '重启次数',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted_at DATETIME DEFAULT NULL COMMENT '删除时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_pod_id (pod_id),
    KEY idx_cluster_namespace (cluster_id, namespace_id),
    KEY idx_tenant_id (tenant_id),
    KEY idx_node_id (node_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='容器Pod表';
```

### 网络模块

```sql
-- VPC表
CREATE TABLE t_cloud_vpc (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT 'VPC ID',
    vpc_id VARCHAR(100) NOT NULL COMMENT 'VPC标识（vpc-xxxxx）',
    vpc_name VARCHAR(200) NOT NULL COMMENT 'VPC名称',
    tenant_id BIGINT NOT NULL COMMENT '所属租户ID',
    region_id BIGINT NOT NULL COMMENT '地域ID',
    cidr_block VARCHAR(50) NOT NULL COMMENT 'CIDR网段',
    description VARCHAR(500) DEFAULT NULL COMMENT '描述',
    route_table_id VARCHAR(100) DEFAULT NULL COMMENT '默认路由表ID',
    status VARCHAR(50) NOT NULL COMMENT '状态：pending/available/modifying/deleting',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted_at DATETIME DEFAULT NULL COMMENT '删除时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_vpc_id (vpc_id),
    KEY idx_tenant_id (tenant_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='VPC表';

-- 子网表
CREATE TABLE t_cloud_subnet (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '子网ID',
    subnet_id VARCHAR(100) NOT NULL COMMENT '子网标识（subnet-xxxxx）',
    subnet_name VARCHAR(200) NOT NULL COMMENT '子网名称',
    vpc_id BIGINT NOT NULL COMMENT '所属VPC ID',
    region_id BIGINT NOT NULL COMMENT '地域ID',
    zone_id BIGINT NOT NULL COMMENT '可用区ID',
    cidr_block VARCHAR(50) NOT NULL COMMENT 'CIDR网段',
    vswitch_name VARCHAR(200) DEFAULT NULL COMMENT '交换机名称',
    description VARCHAR(500) DEFAULT NULL COMMENT '描述',
    route_table_id VARCHAR(100) DEFAULT NULL COMMENT '关联路由表ID',
    available_ip_count INT DEFAULT NULL COMMENT '可用IP数量',
    status VARCHAR(50) NOT NULL COMMENT '状态：pending/available/disabling',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted_at DATETIME DEFAULT NULL COMMENT '删除时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_subnet_id (subnet_id),
    KEY idx_vpc_id (vpc_id),
    KEY idx_zone_id (zone_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='子网表';

-- 安全组表
CREATE TABLE t_cloud_security_group (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '安全组ID',
    security_group_id VARCHAR(100) NOT NULL COMMENT '安全组标识（sg-xxxxx）',
    security_group_name VARCHAR(200) NOT NULL COMMENT '安全组名称',
    tenant_id BIGINT NOT NULL COMMENT '所属租户ID',
    region_id BIGINT NOT NULL COMMENT '地域ID',
    vpc_id BIGINT DEFAULT NULL COMMENT '所属VPC ID（NULL表示默认VPC）',
    description VARCHAR(500) DEFAULT NULL COMMENT '描述',
    status VARCHAR(50) NOT NULL COMMENT '状态：available/modifying',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_security_group_id (security_group_id),
    KEY idx_tenant_id (tenant_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='安全组表';

-- 安全组规则表
CREATE TABLE t_cloud_security_group_rule (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '规则ID',
    security_group_id BIGINT NOT NULL COMMENT '安全组ID',
    direction TINYINT NOT NULL COMMENT '方向：1-入方向，2-出方向',
    priority INT NOT NULL DEFAULT 1 COMMENT '优先级',
    ip_protocol VARCHAR(20) NOT NULL COMMENT '协议：tcp/udp/icmp/all',
    port_range VARCHAR(50) DEFAULT NULL COMMENT '端口范围',
    source_cidr VARCHAR(50) DEFAULT NULL COMMENT '源CIDR（入方向）',
    dest_cidr VARCHAR(50) DEFAULT NULL COMMENT '目标CIDR（出方向）',
    source_group_id BIGINT DEFAULT NULL COMMENT '源安全组ID',
    description VARCHAR(500) DEFAULT NULL COMMENT '描述',
    status VARCHAR(50) NOT NULL DEFAULT 'adding' COMMENT '状态：adding/removing',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (id),
    KEY idx_security_group_id (security_group_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='安全组规则表';

-- 弹性公网IP表
CREATE TABLE t_cloud_eip (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT 'EIP ID',
    eip_id VARCHAR(100) NOT NULL COMMENT 'EIP标识（eip-xxxxx）',
    eip_name VARCHAR(200) NOT NULL COMMENT 'EIP名称',
    tenant_id BIGINT NOT NULL COMMENT '所属租户ID',
    region_id BIGINT NOT NULL COMMENT '地域ID',
    zone_id BIGINT DEFAULT NULL COMMENT '可用区ID',
    ip_address VARCHAR(50) NOT NULL COMMENT '公网IP地址',
    bandwidth INT DEFAULT NULL COMMENT '带宽峰值（Mbps）',
    billing_type TINYINT NOT NULL DEFAULT 1 COMMENT '计费方式：1-按量付费，2-包年包月',
    eip_status VARCHAR(50) NOT NULL COMMENT '状态：Associating/Available/Disassociating',
    instance_id VARCHAR(100) DEFAULT NULL COMMENT '绑定的实例ID',
    instance_type VARCHAR(50) DEFAULT NULL COMMENT '绑定实例类型：vm/container/lb',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted_at DATETIME DEFAULT NULL COMMENT '删除时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_eip_id (eip_id),
    UNIQUE KEY uk_ip_address (ip_address),
    KEY idx_tenant_id (tenant_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='弹性公网IP表';
```

### 存储模块

```sql
-- 云盘表
CREATE TABLE t_cloud_disk (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '云盘ID',
    disk_id VARCHAR(100) NOT NULL COMMENT '云盘标识（disk-xxxxx）',
    disk_name VARCHAR(200) NOT NULL COMMENT '云盘名称',
    tenant_id BIGINT NOT NULL COMMENT '所属租户ID',
    region_id BIGINT NOT NULL COMMENT '地域ID',
    zone_id BIGINT NOT NULL COMMENT '可用区ID',
    disk_type VARCHAR(50) NOT NULL COMMENT '云盘类型：cloud_ssd/cloud_essd/cloud_hdd',
    disk_size INT NOT NULL COMMENT '云盘大小（GB）',
    disk_status VARCHAR(50) NOT NULL COMMENT '状态：Pending/Available/In-use|Deleting/Releasing',
    instance_id VARCHAR(100) DEFAULT NULL COMMENT '挂载的实例ID',
    device_name VARCHAR(50) DEFAULT NULL COMMENT '设备名称（如/dev/vda）',
    delete_with_instance TINYINT NOT NULL DEFAULT 0 COMMENT '随实例释放：0-否，1-是',
    encrypted TINYINT NOT NULL DEFAULT 0 COMMENT '是否加密：0-否，1-是',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted_at DATETIME DEFAULT NULL COMMENT '删除时间',
    expired_at DATETIME DEFAULT NULL COMMENT '到期时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_disk_id (disk_id),
    KEY idx_tenant_id (tenant_id),
    KEY idx_instance_id (instance_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='云盘表';

-- 快照表
CREATE TABLE t_cloud_snapshot (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '快照ID',
    snapshot_id VARCHAR(100) NOT NULL COMMENT '快照标识（snap-xxxxx）',
    snapshot_name VARCHAR(200) NOT NULL COMMENT '快照名称',
    tenant_id BIGINT NOT NULL COMMENT '所属租户ID',
    region_id BIGINT NOT NULL COMMENT '地域ID',
    disk_id VARCHAR(100) DEFAULT NULL COMMENT '源云盘ID',
    disk_snapshot_type VARCHAR(50) DEFAULT NULL COMMENT '云盘快照类型',
    source_disk_size INT DEFAULT NULL COMMENT '源云盘大小',
    source_disk_type VARCHAR(50) DEFAULT NULL COMMENT '源云盘类型',
    snapshot_type TINYINT NOT NULL COMMENT '类型：1-手动快照，2-自动快照',
    retention_days INT DEFAULT NULL COMMENT '保留天数（自动快照）',
    status VARCHAR(50) NOT NULL COMMENT '状态：Pending/Completed/Error',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    deleted_at DATETIME DEFAULT NULL COMMENT '删除时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_snapshot_id (snapshot_id),
    KEY idx_tenant_id (tenant_id),
    KEY idx_disk_id (disk_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='快照表';
```

### 订单与账单模块

```sql
-- 订单表
CREATE TABLE t_cloud_order (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '订单ID',
    order_no VARCHAR(64) NOT NULL COMMENT '订单号',
    tenant_id BIGINT NOT NULL COMMENT '租户ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    order_type TINYINT NOT NULL COMMENT '类型：1-新购，2-续费，3-升级，4-降级',
    resource_type VARCHAR(50) NOT NULL COMMENT '资源类型：vm/container/disk/bandwidth/eip/snapshot',
    resource_id VARCHAR(100) DEFAULT NULL COMMENT '资源ID（如实例ID）',
    region_id BIGINT NOT NULL COMMENT '地域ID',
    spec_id BIGINT DEFAULT NULL COMMENT '规格ID',
    quantity INT NOT NULL DEFAULT 1 COMMENT '数量',
    unit_price DECIMAL(10,4) NOT NULL COMMENT '单价',
    total_amount DECIMAL(12,2) NOT NULL COMMENT '订单总额',
    discount_amount DECIMAL(12,2) NOT NULL DEFAULT 0.00 COMMENT '优惠金额',
    coupon_id BIGINT DEFAULT NULL COMMENT '使用的优惠券ID',
    coupon_amount DECIMAL(12,2) DEFAULT 0.00 COMMENT '优惠券抵扣',
    pay_amount DECIMAL(12,2) NOT NULL COMMENT '实付金额',
    payment_method VARCHAR(50) DEFAULT NULL COMMENT '支付方式：alipay/wechat/balance',
    payment_status TINYINT NOT NULL DEFAULT 0 COMMENT '支付状态：0-待支付，1-已支付，2-已取消，3-已退款',
    payment_time DATETIME DEFAULT NULL COMMENT '支付时间',
    effective_time DATETIME DEFAULT NULL COMMENT '生效时间',
    expire_time DATETIME DEFAULT NULL COMMENT '到期时间',
    status TINYINT NOT NULL DEFAULT 0 COMMENT '订单状态：0-处理中，1-已完成，2-已取消，3-失败',
    remark VARCHAR(500) DEFAULT NULL COMMENT '备注',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_order_no (order_no),
    KEY idx_tenant_id (tenant_id),
    KEY idx_user_id (user_id),
    KEY idx_payment_status (payment_status),
    KEY idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单表';

-- 账单表
CREATE TABLE t_cloud_bill (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '账单ID',
    bill_no VARCHAR(64) NOT NULL COMMENT '账单号',
    bill_type TINYINT NOT NULL COMMENT '类型：1-消费账单，2-充值账单',
    tenant_id BIGINT NOT NULL COMMENT '租户ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    billing_cycle VARCHAR(20) NOT NULL COMMENT '账单周期：YYYY-MM',
    resource_type VARCHAR(50) DEFAULT NULL COMMENT '资源类型',
    resource_id VARCHAR(100) DEFAULT NULL COMMENT '资源ID',
    product_name VARCHAR(200) DEFAULT NULL COMMENT '产品名称',
    specification VARCHAR(200) DEFAULT NULL COMMENT '规格',
    usage_quantity DECIMAL(12,4) DEFAULT 0 COMMENT '用量',
    usage_unit VARCHAR(50) DEFAULT NULL COMMENT '用量单位',
    unit_price DECIMAL(10,4) DEFAULT 0 COMMENT '单价',
    billing_amount DECIMAL(12,2) NOT NULL COMMENT '计费金额',
    discount_amount DECIMAL(12,2) NOT NULL DEFAULT 0.00 COMMENT '优惠金额',
    payable_amount DECIMAL(12,2) NOT NULL COMMENT '应付金额',
    paid_amount DECIMAL(12,2) NOT NULL DEFAULT 0.00 COMMENT '已付金额',
    status TINYINT NOT NULL DEFAULT 0 COMMENT '状态：0-待结算，1-已结算，2-已支付',
    due_date DATETIME DEFAULT NULL COMMENT '到期日期',
    paid_at DATETIME DEFAULT NULL COMMENT '支付时间',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_bill_no (bill_no),
    KEY idx_tenant_billing_cycle (tenant_id, billing_cycle),
    KEY idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='账单表';

-- 账户流水表
CREATE TABLE t_cloud_account_log (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '流水ID',
    log_no VARCHAR(64) NOT NULL COMMENT '流水号',
    tenant_id BIGINT NOT NULL COMMENT '租户ID',
    user_id BIGINT NOT NULL COMMENT '用户ID',
    log_type TINYINT NOT NULL COMMENT '类型：1-充值，2-消费，3-退款，4-冻结，5-解冻',
    amount DECIMAL(12,2) NOT NULL COMMENT '金额',
    balance_before DECIMAL(12,2) NOT NULL COMMENT '变动前余额',
    balance_after DECIMAL(12,2) NOT NULL COMMENT '变动后余额',
    order_no VARCHAR(64) DEFAULT NULL COMMENT '关联订单号',
    bill_no VARCHAR(64) DEFAULT NULL COMMENT '关联账单号',
    payment_channel VARCHAR(50) DEFAULT NULL COMMENT '支付渠道',
    channel_transaction_id VARCHAR(100) DEFAULT NULL COMMENT '渠道交易号',
    remark VARCHAR(500) DEFAULT NULL COMMENT '备注',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_log_no (log_no),
    KEY idx_tenant_id (tenant_id),
    KEY idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='账户流水表';
```

### 监控与告警模块

```sql
-- 告警规则表
CREATE TABLE t_cloud_alarm_rule (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '规则ID',
    rule_name VARCHAR(200) NOT NULL COMMENT '规则名称',
    tenant_id BIGINT NOT NULL COMMENT '租户ID',
    region_id BIGINT NOT NULL COMMENT '地域ID',
    resource_type VARCHAR(50) NOT NULL COMMENT '资源类型：vm/container/disk',
    resource_id VARCHAR(100) NOT NULL COMMENT '资源ID',
    metric_name VARCHAR(100) NOT NULL COMMENT '监控指标：cpu_usage/memory_usage/disk_usage',
    condition_type VARCHAR(50) NOT NULL COMMENT '条件类型：gt/gte/lt/lte/eq',
    threshold DECIMAL(10,2) NOT NULL COMMENT '阈值',
    period INT NOT NULL DEFAULT 300 COMMENT '检测周期（秒）',
    period_count INT NOT NULL DEFAULT 3 COMMENT '连续周期数',
    severity TINYINT NOT NULL DEFAULT 1 COMMENT '告警级别：1-信息，2-警告，3-严重',
    contact_group_ids VARCHAR(200) DEFAULT NULL COMMENT '联系人组ID列表',
    status TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-停用，1-启用',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    PRIMARY KEY (id),
    KEY idx_tenant_resource (tenant_id, resource_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='告警规则表';

-- 告警记录表
CREATE TABLE t_cloud_alarm_event (
    id BIGINT NOT NULL AUTO_INCREMENT COMMENT '告警ID',
    alarm_id VARCHAR(100) NOT NULL COMMENT '告警事件ID',
    rule_id BIGINT NOT NULL COMMENT '规则ID',
    rule_name VARCHAR(200) NOT NULL COMMENT '规则名称',
    tenant_id BIGINT NOT NULL COMMENT '租户ID',
    region_id BIGINT NOT NULL COMMENT '地域ID',
    resource_type VARCHAR(50) NOT NULL COMMENT '资源类型',
    resource_id VARCHAR(100) NOT NULL COMMENT '资源ID',
    resource_name VARCHAR(200) DEFAULT NULL COMMENT '资源名称',
    metric_name VARCHAR(100) NOT NULL COMMENT '监控指标',
    metric_value DECIMAL(10,2) NOT NULL COMMENT '指标值',
    threshold DECIMAL(10,2) NOT NULL COMMENT '阈值',
    severity TINYINT NOT NULL COMMENT '告警级别',
    alarm_status TINYINT NOT NULL DEFAULT 0 COMMENT '状态：0-触发，1-已通知，2-已恢复，3-已忽略',
    triggered_at DATETIME NOT NULL COMMENT '触发时间',
    notified_at DATETIME DEFAULT NULL COMMENT '通知时间',
    recovered_at DATETIME DEFAULT NULL COMMENT '恢复时间',
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    PRIMARY KEY (id),
    UNIQUE KEY uk_alarm_id (alarm_id),
    KEY idx_rule_id (rule_id),
    KEY idx_tenant_status (tenant_id, alarm_status),
    KEY idx_triggered_at (triggered_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='告警记录表';
```

## 完整ER图

```
┌──────────────┐     ┌────────────────┐     ┌──────────────┐
│ t_cloud_user│────▶│ t_cloud_tenant │◀────│t_cloud_user_tenant│
└──────────────┘     └───────┬────────┘     └──────────────┘
                              │
         ┌───────────────────┼───────────────────┐
         │                   │                   │
         ▼                   ▼                   ▼
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│t_cloud_instance│   │t_cloud_container│   │ t_cloud_disk │
│  (云服务器)   │     │   (容器)       │     │   (云盘)     │
└───────┬──────┘     └───────┬──────┘     └───────┬──────┘
        │                   │                   │
        └───────────────────┼───────────────────┘
                            │
                            ▼
                    ┌──────────────┐
                    │ t_cloud_vpc  │
                    │   (网络)      │
                    └──────────────┘

┌──────────────┐     ┌──────────────┐
│t_cloud_order │────▶│ t_cloud_bill │
│   (订单)      │     │   (账单)     │
└──────────────┘     └──────────────┘
```

## 索引设计总结

| 表名 | 索引名称 | 索引字段 | 用途 |
|------|---------|---------|------|
| t_cloud_user | uk_username | username | 用户名唯一 |
| t_cloud_tenant | uk_tenant_code | tenant_code | 租户代码唯一 |
| t_cloud_instance | uk_instance_id | instance_id | 实例标识唯一 |
| t_cloud_instance | idx_tenant_status | tenant_id, instance_status | 查询租户实例 |
| t_cloud_container_cluster | uk_cluster_id | cluster_id | 集群标识唯一 |
| t_cloud_container_pod | uk_pod_id | pod_id | Pod标识唯一 |
| t_cloud_vpc | uk_vpc_id | vpc_id | VPC标识唯一 |
| t_cloud_disk | uk_disk_id | disk_id | 云盘标识唯一 |
| t_cloud_eip | uk_ip_address | ip_address | IP地址唯一 |
| t_cloud_order | uk_order_no | order_no | 订单号唯一 |
| t_cloud_bill | uk_bill_no | bill_no | 账单号唯一 |

---

**创建时间**：2026-03-21
**文档版本**：v1.0
